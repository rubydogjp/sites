import 'package:flutter/material.dart';

class TweetSeparater extends StatelessWidget {
  const TweetSeparater({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 0.2,
      color: Colors.grey,
    );
  }
}
